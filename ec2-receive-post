from flask import Flask, request
main = Flask(__name__)

#/にpost通信が来たら実行（ルーティング）
@main.route('/', methods=['post'])
def receive():
    print(request.json)
    return 'OK'

#hostはＩＰアドレスを示しており、全てのＩＰアドレスを許可している
main.run(host='0.0.0.0', port=5000)
